{
  "name": "backend",
  "version": "1.0.0",
  "description": "Realtime Group App Backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js || node server.js",
    "check:merge-markers": "node scripts/check-merge-markers.js",
    "prestart": "npm run check:merge-markers && node --check routes/auth.js",
    "predev": "npm run check:merge-markers && node --check routes/auth.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.6.0",
    "socket.io": "^4.7.2",
    "nodemailer": "^6.9.13",
    "twilio": "^5.0.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
