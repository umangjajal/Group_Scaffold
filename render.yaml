services:
  - type: web
    name: realtime-group-app-backend
    runtime: node
    rootDir: backend
    buildCommand: bun install && bun run build
    startCommand: bun run start
    healthCheckPath: /health
